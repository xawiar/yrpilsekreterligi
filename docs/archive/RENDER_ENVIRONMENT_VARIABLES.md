# ✅ Render.com - Environment Variables (DEĞİŞMEYECEK)

## 📋 Environment Variables

**Root Directory değişikliği Environment Variables'ı ETKİLEMEZ!**

Aynı değerleri kullanmaya devam edin:

---

## 🔧 RENDER.COM AYARLARI

### Settings → Environment Variables:

#### 1. VITE_USE_FIREBASE

**Key:**
```
VITE_USE_FIREBASE
```

**Value:**
```
true
```

**⚠️ ÖNEMLİ:** 
- Key alanına sadece `VITE_USE_FIREBASE` yazın
- Value alanına sadece `true` yazın
- Tırnak işareti EKLEMEYİN!

---

#### 2. VITE_ENCRYPTION_KEY

**Key:**
```
VITE_ENCRYPTION_KEY
```

**Value:**
```
ilsekreterlik-app-encryption-key-2024-secret-very-long-key-for-security-minimum-32-characters
```

**⚠️ ÖNEMLİ:** 
- Key alanına sadece `VITE_ENCRYPTION_KEY` yazın
- Value alanına sadece değeri yazın (yukarıdaki uzun string)
- Tırnak işareti EKLEMEYİN!
- Eşittir işareti (=) EKLEMEYİN!

---

## ✅ ÖZET

**Environment Variables:**
- ✅ **VITE_USE_FIREBASE** = `true`
- ✅ **VITE_ENCRYPTION_KEY** = `ilsekreterlik-app-encryption-key-2024-secret-very-long-key-for-security-minimum-32-characters`

**Değişiklik YOK!** Aynı değerleri kullanmaya devam edin!

---

## 📋 TÜM AYARLAR ÖZET

### Root Directory (DEĞİŞTİ):
```
sekreterlik-app/client
```

### Build Command (DEĞİŞTİ):
```
npm install && npm run build
```

### Publish Directory (DEĞİŞTİ):
```
dist
```

### Environment Variables (DEĞİŞMEDİ):
- ✅ **VITE_USE_FIREBASE:** `true`
- ✅ **VITE_ENCRYPTION_KEY:** `ilsekreterlik-app-encryption-key-2024-secret-very-long-key-for-security-minimum-32-characters`

---

**Environment Variables değişmeyecek, aynı değerleri kullanın!** ✅

